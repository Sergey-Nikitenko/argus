# runtime data (detections, quarantine, watcher findings — machine-local, never committed)
data/
demo-data/
.smoke-data/

# build artifacts
dist/
build/
*.spec

# python
__pycache__/
*.pyc
*.pyo
.venv/

# red-team / offensive tooling — never publish
redteam/

# sandbox staging (binaries, passwords, screenshots, machine-specific paths) — never publish
sandbox-staging/

# test cache + logs
.pytest_cache/
*.log

# front-end icon tooling + design specs (dev artifacts � keep local, not published)
tools/
design/
